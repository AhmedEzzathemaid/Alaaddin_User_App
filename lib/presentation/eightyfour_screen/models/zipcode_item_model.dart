import '../../../core/app_export.dart';/// This class is used in the [zipcode_item_widget] screen.
class ZipcodeItemModel {ZipcodeItemModel({this.zipcode, this.id, }) { zipcode = zipcode  ?? "2000";id = id  ?? ""; }

String? zipcode;

String? id;

 }
