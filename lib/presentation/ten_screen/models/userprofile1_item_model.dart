import '../../../core/app_export.dart';/// This class is used in the [userprofile1_item_widget] screen.
class Userprofile1ItemModel {Userprofile1ItemModel({this.defaultAddress, this.addressText, this.id, }) { defaultAddress = defaultAddress  ?? "Default Address";addressText = addressText  ?? "St7, Shabwa, Dar Muhaimud, Yemen";id = id  ?? ""; }

String? defaultAddress;

String? addressText;

String? id;

 }
